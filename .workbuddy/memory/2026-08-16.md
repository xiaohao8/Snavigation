# 2026-08-16 工作日志

## 暗色模式加深 + 按时段自动切换主题

- **暗色更黑**（css/style.css :root）：`--body-background-color` #1a1622→#08070c、`--main-background-color` #120f17→#0b0910、hover #1e1a26→#16121d、active #2a2436→#1d1826；壁纸 `#bg` brightness 0.55→0.32（玻璃主题 0.8→0.68）。theme.css 暗色预览卡同步调黑。
- **自动切换主题**（js/theme.js 重写）：新增 cookie `theme_auto`(1/0)、`theme_auto_from`(默认18:30)、`theme_auto_to`(默认07:00)。开启后按系统时间在 暗色(夜间)/玻璃(白天) 间自动切换，跨午夜用 `from>to` 判断；每 30s 定时检查；手动点主题卡片则关闭自动（theme_auto=0）。导出 ThemeManager.{get,set,apply,isAuto,enableAuto,disableAuto}。
- **设置页外观 tab 加自动切换区**（index.html + theme.css）：开关(toggle 用 aria-checked)+ 两个 time 输入(进入暗色/回到玻璃)+ 说明，时间输入默认禁用、开启后启用、change 即时生效。
- **验证**：jsdom 手工 DOM + eval 源码（真实 js-cookie + mock Date）27 项 PASS（默认态、夜间→dark、白天→glass、跨午夜边界 6 组、手动点卡片关自动、同日内时段、改时间生效）+ fromFile 集成 15 项 PASS（UI 结构、暗色变量 #0b0910、壁纸 brightness 0.32/0.68、切玻璃/暗色）。

**关键坑**：jsdom 手动 `w.eval()` 时 `document.readyState === 'loading'`，theme.js 的 init 走 DOMContentLoaded 延迟执行——测试必须 `await DOMContentLoaded` 后再断言；此前 mock `window.Cookies` 会被页面里真实加载的 js.cookie.js 覆盖，需用独立手工 DOM + eval 源码验证。

## 三页面板/卡片暗色统一

- **色差根因**：书签面板 `#bookmark-panel`（css/animated-list.css）是硬编码旧色（面板 #120F17、条目 #2F293A、selected #3a3350、header/footer #16121d、滚动条/渐变 #120F17），而快捷方式页 `.mark`/设置页 `.set` 已用变量 `#0b0910`。
- **统一方案**（暗色三级层次，全部改引用 CSS 变量）：
  - 面板背景 = `--main-background-color` #0b0910（.mark/.set 原本如此；.bm-panel 从 #120F17 改引用）
  - 卡片/列表项默认 = `--main-background-hover-color` #16121d（.quick 从 main-bg 改 hover；.se_list_div/.quick_list_div 从 main-bg 改 hover；.bm-item 从 #2F293A 改引用）
  - hover/selected = `--main-background-active-color` #1d1826（.quick:hover/.se_list_div:hover 从 hover 改 active；.bm-item 新增 :hover；.bm-item.selected 从 #3a3350 改引用）
  - 书签面板 header/footer、滚动条、渐变遮罩、border 全部改 rgba/变量，消除硬编码。
- **验证**：jsdom 读取 cssRules 13/14 PASS（变量值 #0b0910/#16121d/#1d1826、三页卡片/hover 全引用一致变量、无 #120F17/#2F293A 硬编码残留）；唯一 FAIL 是测试脚本 `.mark, .set` 换行选择器 indexOf 匹配不到（实际 style.css 496 行已确认 `background: var(--main-background-color)`）。
- **玻璃主题**（theme.css `body[data-theme="glass"] #bookmark-panel`）用 rgba 半透明覆盖，未受影响。

## 恢复 #120f17（用户纠正：书签页原黑色才是对的）

用户反馈「不要动原来的黑色书签页，改回去」——前面调黑（#0b0910）过度了，#120f17 才是认可的"黑色"。全部撤销「三页统一」轮 + 「暗色加深」轮的面板改动：
- **变量恢复**（style.css :root）：`--main-background-color` #0b0910→#120f17、hover #16121d→#1e1a26、active #1d1826→#2a2436；body #08070c 与壁纸 brightness 0.32（全局暗）**保留**（那是「全局调暗」的正确部分）。
- **书签面板恢复原硬编码**（animated-list.css）：.bm-panel #120F17、header/footer #16121d、.bm-item #2F293A、selected #3a3350、滚动条/渐变 #120F17/#2F293A、border #222/#333，删除新增的 .bm-item:hover。
- **卡片恢复与面板同色**（style.css）：.quick / .se_list_div / .quick_list_div 默认 background 回 `var(--main-background-color)`=#120f17，hover 回 #1e1a26。
- theme.css 暗色预览卡回 #120f17 系渐变。
- **验证**：变量值 4 项 PASS（#120f17/#1e1a26/#2a2436 + body #08070c 保留）+ 三页面板/卡片引用 4 项 PASS + 无 #0b0910 残留。4 项「FAIL」实为 jsdom 把 hex 转 rgb（rgb(47,41,58)=#2F293A、rgb(58,51,80)=#3a3350 等），grep 源码已确认硬编码全部恢复。
- **结论**：三页面板背景统一 #120f17（.mark/.set 用变量、#bookmark-panel 用硬编码，渲染一致）；全局背景仍暗（body #08070c + 壁纸 brightness 0.32）。

## 暗色主页改纯黑（壁纸隐藏）

用户反馈「暗色模式主页图片显示不了就改成纯黑」——暗色下壁纸被 brightness 0.32 压得太暗几乎看不见，干脆纯黑；液态玻璃保持原样显示壁纸。
- **style.css**：`--body-background-color` #08070c→#000000（纯黑）；`#bg` 默认加 `opacity: 0`（暗色隐藏壁纸）；`body[data-theme="glass"] #bg` 加 `opacity: 1` + 保持 brightness(0.68)。
- 壁纸切换只用 `$('#bg').attr('src')` 不碰 opacity，无冲突；`transition: opacity 1s` 是预留的。
- **验证**：jsdom 6 项 PASS（body #000000、暗色壁纸 opacity 0、面板仍 #120f17、玻璃 opacity 1 + brightness 0.68、切回暗色隐藏）。

## 暗色模式：纯黑主页（无壁纸）+ 三页面板统一 #120F17（本轮加固）

用户再次确认暗色模式要「纯黑主页无壁纸 + 三页面板统一 #120F17」。在前述基础上做两处显式加固（css/style.css）：
- **无壁纸更彻底**：`body:not([data-theme="glass"]) .bg-all { display: none }`——暗色下整个壁纸层（含 `#bg` 图与 `.cover` 遮罩）不渲染、不发起图片请求；玻璃主题仍走 `body[data-theme="glass"] #bg { opacity:1 }` 显示壁纸，不受影响。
- **三页面板显式统一 #120F17**：`body:not([data-theme="glass"]) .all-search, #keywords, .mark, .set { background-color: #120F17 }`，把「首页搜索 / 快捷方式 / 设置」三页面板在暗色下强制为同一色值（此前靠 `--main-background-color` 变量推导，现已写死保证统一），玻璃主题半透明面板不变。
- 预览：本地 `python -m http.server 8080` 已起，http://127.0.0.1:8080/ 可看效果。
