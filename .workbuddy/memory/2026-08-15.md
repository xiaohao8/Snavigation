# 2026-08-15 工作日志

## 右上角设置按钮 → AnimatedList 书签面板（React Bits AnimatedList 原生复刻）

- **新增文件**：
  - `css/animated-list.css`：`#bookmark-panel` 右上角浮层（top:64px right:16px, 420px, 深色圆角阴影）+ 滚动列表（渐变遮罩 top/bottom、自定义滚动条）+ 条目（入场 scale 0.7→1 + opacity 过渡、hover/选中高亮）+ 空状态 + 管理按钮
  - `js/animated-list.js`：`AnimatedList.init(container, opts)` 组件——滚动渐变 opacity（scrollTop/50 与 bottom 距离/50）、hover/点击选中、window 键盘导航（Arrow/Tab 上下 + Enter 确认，与 React Bits 一致）、选中项自动滚动到可视区（extraMargin 50，兼容无 scrollTo 环境）、入场动画 stagger（rAF + .visible）；`BookmarkPanel` 面板管理（open/close/toggle/refresh，数据源 getQuickList 与快捷方式同源 33 项，点击条目 window.open 跳转 + 关闭）
- **改动**：
  - `index.html`：引入 animated-list.css/js
  - `js/set.js`：#menu 点击从打开设置面板改为 `BookmarkPanel.toggle()`（未加载时回退设置面板）；openSet/closeSet 移除对 #menu class/图标的操作；新增全局 `openBookmarkManage()`（打开设置面板 + setQuickInit + 切到"快捷方式"tab）
  - `js/dock.js`：settings 动作从 menu.click() 改为直接 openSet/setSeInit/setQuickInit toggle（避免触发书签面板）
- **经验**：jsdom 通过 HTTP 服务器加载脚本**偶发丢脚本**（函数 undefined）——改用 `JSDOM.fromFile` 本地加载后稳定；`window.open` 在 beforeParse 覆盖也会干扰 jsdom 脚本执行，测试应加载后拦截
- **验证**：fromFile 加载 jsdom 12 项断言全 PASS（打开/关闭面板、33 项渲染、点击跳转关闭、键盘选中+Enter、管理书签→设置快捷方式 tab、Dock 设置 toggle）

## 主题系统：暗色 / 液态玻璃（UI Designer 专家会话）

- **需求**：书签页做成透明（与 dock 一致）=「液态玻璃」主题；设置页加「外观」tab 双主题切换（暗色默认 / 玻璃）；暗色模式要全局适配
- **新增文件**：
  - `css/theme.css`：`body[data-theme="glass"]` 覆盖——语义色令牌（--main-background-* 等 → rgba 白）+ 主要面板（.mark/.set/.all-search/#keywords/#bookmark-panel）rgba 白底 + `backdrop-filter: blur(24px) saturate(1.5)` + 细白边框 + 阴影；面板内次级元素（.quick/.se_list_div/.bm-item/输入/按钮）半透明白；书签面板渐变遮罩同步换色；「外观」设置 UI（.theme-options/.theme-card：预览块 dark 深色渐变、glass 透明+光泽扫过动画、选中橙色高亮 #f4a759、hover 上浮）
  - `js/theme.js`：`ThemeManager`（get/set/apply）——cookie `theme`（默认 dark，仅接受 dark/glass）；apply 设置/移除 body[data-theme] + 同步卡片高亮；click/键盘 Enter 委托切换；DOMContentLoaded 初始化
- **改动**：`index.html` 设置页 tabs 加「外观」（第 5 个 tab）+ mainConts 两个主题卡片（data-theme-option），引入 theme.css/js
- **关键经验**：jsdom `fromFile`（file://）下 **document.cookie 写入无效**（cookie 需有效域名）——cookie 链路必须用 http URL 验证；jsdom http 加载抖动需重试直到 ThemeManager/Cookies 就绪
- **验证**：fromFile 10 项 PASS（默认 dark、外观 tab/卡片、切换 glass 半透明背景、卡片高亮）+ http 环境 6 项 PASS（cookie 保存/更新）+ 持久化恢复 3 项 PASS（带 theme=glass 加载自动应用）；js-cookie 独立读写 PASS

## 液态玻璃主题重构 + Cookie 提示 + 移动端适配（UI Designer 专家会话）

- **设计依据**：搜索学习 Apple Liquid Glass 规范（HIG/WWDC）——玻璃只用于浮层/控件层避免玻璃叠玻璃；分层 Standard（面板）/Controls（控件）；靠高光+内边框亮线+投影做深度而非颜色；tint 克制；对比度优先
- **重构 `css/theme.css`**：玻璃材质升级——面板 `.mark/.set/#bookmark-panel` = `blur(34px) saturate(1.8) brightness(1.06)` + 多层背景（`--glass-tint` 淡冷色高光 radial + `--glass-bg-panel` 135° 渐变）+ 1px 白边框 + `inset 顶部亮线` + 深阴影；搜索框/建议 = Controls 薄玻璃（blur 26）；面板内卡片**不做玻璃**（避免叠玻璃）用 rgba 白 + 顶部内高光保证可读；书签条目 hover 微放大、选中琥珀辉光；Tab 栏细分割线
- **Cookie 提示**（`js/cookie-tip.js` + HTML `#cookie-tip` + CSS）：首次访问（无 `cookie_ack` cookie）延迟 1.2s 右下角弹出，提示书签/引擎/壁纸/主题存在 Cookie 中勿清除；点"知道了"写 `cookie_ack`（30 天）不再显示；玻璃风格弹窗（blur 30 + 高光 + 琥珀主按钮）
- **移动端适配**（theme.css `@media (max-width:640px)`）：con padding-top 8vh；`.mark/.set` 近全宽 `calc(100%-24px)`、圆角 16px、max-height 68vh 可滚；快捷方式网格 2 列（46%）；外观卡片纵向堆叠；书签面板改为底部全宽（left/right 12px）；Cookie 提示底部通栏；时间 2.2rem；Dock 44px
- **验证**：Cookie 提示 http 环境 6 项 PASS（首访显示/确认隐藏+cookie 写入/已确认不再显示）+ 主题回归 7 项 PASS（切换 glass、毛玻璃 blur 生效、外观卡片/Cookie 元素存在；1 项 jsdom 不解析 CSS 变量引用属环境限制）

## 图标系统重构（iconfont 字体 → 内联 SVG 精灵图，UI Designer 专家会话）

- **选型**：功能图标 **Lucide**（线条风，契合玻璃主题）+ 品牌图标 **simple-icons**（品牌标准形）——均自托管，不依赖网络；无品牌 SVG 的必应/京东/360 与自定义引擎 → **首字徽章**（液态玻璃质感圆角方块）
- **下载源**：lucide GitHub raw（新版命名 settings/house 等）+ simple-icons develop 分支；bing/jd/360/weibo(→sinaweibo) 命名已核对
- **新增**：
  - `index.html` 内联 sprite：19 个 `<symbol>`（11 Lucide + 8 品牌），`<use href="#ic-xxx">`
  - `js/icons.js`：`Icons.svg/engine/ui/renderInto`——品牌映射（BRAND_ICONS by title）、旧 iconfont class 兼容（LEGACY_MAP，含 icon-wangluo 占位判定）、首字徽章
  - CSS：`.isvg`（inline-block）+ `.ibadge`（渐变高光圆角方块，玻璃主题更通透）
- **改造**：引擎按钮（renderEngineIcon → 品牌 SVG/徽章）、搜索按钮、右上角 #menu、Dock 三图标、Cookie 提示、设置列表按钮（home/pencil/trash SVG）、快捷方式 add 按钮、书签管理按钮——全部 SVG；iconfont 保留为回退（Icons 未加载时）+ LEGACY_MAP 兼容历史 cookie
- **坑**：Git Bash `/tmp` 与 node 路径不一致（/tmp = AppData\Local\Temp）；heredoc 内 JS 模板字符串会被 shell 展开（改用 Write 写脚本）
- **验证**：jsdom 15 项 PASS（sprite 19 symbols、百度/搜狗→品牌 SVG、必应→B 徽章、自定义→首字徽章、设置列表 33/66 SVG、Dock/menu/搜索/书签管理 SVG）

## 主题明暗度调整（暗色更暗 / 玻璃参照暗色透明度）

- **需求**：玻璃主题太刺眼——按原暗色设置面板透明度（40%）做深色玻璃；暗色模式更暗
- **改动**：
  - `css/style.css` :root（暗色默认）：`--main-background-color` `#00000040` → **`#120f17` 实心深色**；hover/active → `#1e1a26`/`#2a2436`；body 背景 `#333` → `#1a1622`；按钮/输入半透明白微调；阴影加深
  - `css/theme.css` 玻璃主题：面板/搜索框/书签面板从**亮白玻璃**（rgba 白 0.05+0.14 高光+blur34 saturate1.8 brightness）→ **深色磨砂**：`rgba(13,11,19,0.40)`（参照暗色 40% 透明度）+ 极弱白渐变（0.045）+ tint 减半（0.05/0.03）+ 弱内高光（0.07）+ blur 28 saturate 1.4（去掉 brightness）；渐变遮罩改深色系；`.ibadge` 亮白徽章 → 深色玻璃徽章（暗色下深底、玻璃下透明）；外观预览卡 .glass 同步深色
- **验证**：jsdom 7 项 PASS（暗色变量 #120f17/#1e1a26/#1a1622、.mark/.set 引用变量；玻璃 40% 深色半透明、书签页同步、磨砂保留——2 项 computed 返回 var() 原值属 jsdom 不解析 CSS 变量，真实浏览器正常）

## 图标系统 v2：品牌色 + favicon（适配网站导航）

- **需求**：单色线条 SVG 不适合网站导航——换高辨识度方案
- **方案**：
  - 引擎图标：品牌 SVG + **官方品牌色**（simple-icons `_data` 提取：baidu #2932E1、google #4285F4、sogou #FB6022、zhihu #0084FF、sinaweibo #E6162D、taobao #E94F20、bilibili #00A1D6、github 用白）
  - 无品牌 SVG（必应 B/京东 J/360 3）：**品牌色首字/字母徽章**（`.ibadge--brand`，官方字母标更贴品牌）；自定义引擎：配色池首字徽章
  - **网站快捷方式/书签**：favicon（**dnspod proxy_favicon** 主源 → **favicon.im** 回退，Google s2 被墙弃用）+ **品牌色渐变首字底**（favicon 加载失败露出）；配色池 12 色按名称 hash 稳定分配
  - 快捷方式卡布局：横向文字块 → **纵向图标卡**（favicon 40px + 名称小字，hover 上浮）；书签面板条目同步 favicon
- **改动**：`js/icons.js`（BRAND_ICONS 带色、BADGE_COLORS、COLOR_POOL、siteIcon/faviconUrl/extractDomain）、`css/style.css`（.quick 纵向卡 96px）、`css/theme.css`（.q-icon/.q-favicon/.q-icon-char/.ibadge--brand）、`js/set.js` quickData、`js/animated-list.js` bm-item
- **验证**：jsdom 14 项 PASS（品牌色 SVG/徽章、favicon dnspod 源+域名提取、快捷方式 34 卡、书签条目 favicon；京东徽章显示官方字母 J 属预期）

## 快捷方式/书签分离 + favicon 配置（UI Designer 专家会话）

- **数据分离**：新 `bookmark_list` cookie（用户完全自建），快捷方式 `quick_list`（系统推荐可编辑）不变；`getBookmarkList/setBookmarkList` 独立存取
- **设置页「快捷方式」tab 上下分区**：`.set-section` 上半 `#section-quick`（快捷方式管理）+ 下半 `#section-bookmark`（书签管理，空态提示）；`.set` 加 overflow-y auto 支持内部滚动定位
- **favicon 配置**（所有添加/编辑表单：引擎、快捷方式、书签）：`name=icon` 输入框 + 「图标库」按钮 → `js/icon-picker.js` 弹窗（11 品牌网格：8 SVG + 3 字母徽章，选 `si:品牌`；可粘贴 URL；清除）；`icons.js` 解析 `si:`（白底品牌 SVG）/URL（白底 img）/空（自动 dnspod favicon + 品牌色首字底）
- **跳转定位**：`openQuickAdd()`（首页快捷方式添加 → 设置快捷方式区+表单）、`openBookmarkAdd()`（书签面板「添加书签」按钮 → 设置书签区+表单）、`openBookmarkManage()` 滚动定位 `scrollToSetSection`
- **渲染打通**：快捷方式卡/书签面板条目/三个设置列表均优先 favicon 字段（si:/URL/自动）；`engineIcon` 支持 si:/URL
- **验证**：http 环境 jsdom 6+6 项 PASS（快捷方式 key5 与书签 key5 完全独立、icon 字段保存、openQuickAdd/openBookmarkAdd 跳转、书签列表品牌图标、书签面板仅用户数据+添加按钮、图标库 11 项）——file:// 下 cookie 禁用故用 http 验证

## 恢复原 iconfont + 保留图标配置系统

- **需求**：功能/引擎图标恢复原 iconfont 字体（用户熟悉样式），保留 favicon 配置系统（表单字段 + 图标库弹窗 + si:/URL 解析）
- **改动**：
  - `index.html` 静态图标 SVG → iconfont（#icon-sou icon-sousuo、#menu icon-shezhi、Dock icon-home/wangluo/shezhi、Cookie icon-baobei）
  - `js/set.js` setSeInit/setQuickInit/setBookmarkInit/quickData 图标变量 SVG → iconfont（icon-home/xiugai/delete/tianjia-）；`renderEngineIcon` 改为：si:/URL 配置 → html 注入 SVG/img，否则 attr class（原 iconfont）
  - `js/icons.js` engineIcon：si:品牌 → SVG / URL → img / 默认 → `<i class="iconClass">`（原 iconfont）；siteIcon 不变（favicon 自动 + 配置优先）
  - `js/animated-list.js` manage/add 按钮 SVG → iconfont（icon-shezhi/tianjia-）
  - SVG sprite 保留在 index.html（图标库弹窗品牌预览 + si: 渲染用）
- **验证**：jsdom 12 项 PASS（功能图标 iconfont、引擎默认 icon-baidu、配置 si:google→SVG、配置 URL→img、恢复默认、设置列表 iconfont、快捷方式卡 favicon 保留、图标库弹窗 11 品牌）

## 暗色模式全局暗化（壁纸滤镜）

- **需求**：暗色模式只有面板深色、壁纸太亮全局不统一 → 给壁纸加暗色滤镜
- **改动**（`css/style.css`）：`#bg` filter 加 `brightness(0.55)`（暗色全局变暗）；`body[data-theme="glass"] #bg` brightness(0.8)（玻璃稍亮保留透出感）；filter transition 0.5s 平滑切换
- **验证**：jsdom 3 项 PASS（暗色 brightness 0.55、玻璃 0.8、无模糊）

## 修复书签面板打不开 + 快捷方式图标缩小

- **bug**：上轮编辑 animated-list.js 时漏掉 `var footer` 声明 → buildPanel 抛 `footer is not defined` → 书签面板打不开；补回 `var footer = el('div','bm-panel-footer')`
- **图标尺寸统一**：快捷方式卡 favicon 40px → 32px（`quickData`），书签面板条目 30px → 28px（协调）
- **验证**：jsdom 4 项 PASS（面板打开、无 JS 错误、添加/管理按钮、图标 32px）
