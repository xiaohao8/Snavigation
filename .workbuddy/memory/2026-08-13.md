# 2026-08-13 工作日志

## 删除开屏动画 & 优化加载速度

- 移除了 `index.html` 中的 `#loading-box` 开屏动画 HTML 结构
- 移除了 `index.html` 对 `css/loading.css` 的 `<link>` 引用（文件本身保留但不再加载）
- 将 51.la 统计脚本改为 `async` 加载，不阻塞渲染
- `css/style.css` 中将 `section` 从 `opacity:0` 改为 `opacity:1`，移除 1.5s 过渡
- `css/style.css` 中将 `#bg` 初始状态从 `scale(1.10)+blur(10px)` 改为 `scale(1)+blur(0)`，内容立即可见
- `css/style.css` 中将 `.cover` 从 `opacity:0` 改为 `opacity:1`
- `js/main.js` 中删除了 `window.load` 里的载入动画代码（`$('#loading-box')` 及 bg/section/cover 的渐显），保留欢迎 toast 和字体加载
- 效果：页面内容不再等待 `window.load`（所有资源下载完）才显示，而是随 HTML/CSS 解析立即可见

## 修复：页面功能全挂（时间不显示、按钮点不动）——CDN 失效

- **根因**：jQuery 与 iziToast 引用的字节跳动 CDN（`lf26/lf3-cdn-tos.bytecdntp.com`）链接已失效（jQuery 返回 404、iziToast 超时），页面所有交互依赖 jQuery → 整个页面 JS 瘫痪
- **修复**：将 jQuery 3.6.0、iziToast 1.4.0（js+css）下载到本地 `js/lib/` 目录，`index.html` 改为引用本地文件，彻底摆脱对失效 CDN 的依赖
- 51.la 统计脚本 `LA.init` 包了 try/catch（SDK 异步加载失败时不抛错中断）
- **经验**：此类"全站功能瘫痪但页面能打开"问题，先用 curl 检查外部 CDN 可达性（`curl -o /dev/null -w "%{http_code}" <url>`）；本地化下载用 bootcdn（`cdn.bootcdn.net`）在国内网络最稳
- 下载源：jquery 3.6.0 89KB、iziToast.min.js 18KB、iziToast.min.css 41KB（CSS 内图片均为 base64 内联，无外部依赖）

## 修复：背景图片功能失效 + 全面功能检查

- **根因**：背景图片 3 个外部 API 全部失效——`api.dujin.org`(必应每日) 521 宕机、`api.ixiaowai.cn`(风景/二次元) 连接失败；图片失败后 `onerror` 直接把背景隐藏（`img.error{display:none}`）→ 页面无背景
- **修复**（`js/set.js`）：
  - 默认壁纸类型改为 `"1"`（本地 10 张 webp 随机，零依赖）
  - 每日必应 → 官方接口 `https://cn.bing.com/HPImageArchive.aspx?format=js&idx=0&n=1`（fetch JSON 拼 `https://cn.bing.com`+url）
  - 随机风景 → 必应历史图库（idx 0-7 随机，均为自然风光）
  - 随机二次元 → `https://www.dmoe.cc/random.php`（HTTPS 302→image/jpeg）
  - 新增 `bgFallback()`：任何外部图加载失败自动降级本地随机壁纸；`index.html` 的 `#bg` onerror 改调 `bgFallback()`
  - `getBgImg()` 加 try/catch 防 cookie 损坏 JSON 导致初始化中断
- **天气**（`js/main.js`）：`yiketianqi.com` 只支持 HTTP（HTTPS 握手失败），`https://` 改为 `http://`；本地/file 部署下天气恢复，https 部署下仍会被 mixed content 拦截
- **检查确认正常**：搜索建议接口 `suggestion.baidu.com`(200)、字体文件齐全、js.cookie.js 语法正常、搜索引擎/书签链接均为静态配置无加载问题
- **注意**：用户浏览器中已有 `bg_img` cookie（旧 type 2/3/4），切到新 API 后若失败会自动降级本地壁纸，无需手动清理

## 需求：快捷方式在首页展示

- `index.html`：`.mark`（书签/快捷方式面板）去掉内联 `style="display: none;"`，默认参与布局
- `js/set.js`：`$(document).ready` 初始化中调用 `openBox()`，页面加载即进入"书签打开"状态（书签面板 flex 显示 + 时间上移 + 背景模糊 + 搜索框淡出，均为原设计布局）
- `closeSet()`：原来关闭设置后调用 `closeBox()`（书签隐藏），改为 `openBox()`（关闭设置后书签恢复显示）
- 交互保留：点时间仍可切换书签开/关（openBox/closeBox），原逻辑未破坏
- 验证：jsdom 加载页面确认 `.mark` display=flex、`#content` 有 box class、本地壁纸正常加载

## 修复：快捷方式遮挡搜索框 + 收起无效 → 重构为文档流布局

- **根因**：① `.mark` 原为 `position:absolute; margin-top:200px; z-index:2000` 悬浮面板，直接叠在搜索框上；② 时间上移 `translateY(-160%)` 后被书签面板盖住点不到；③ 时间点击的收起逻辑里 `closeBox()` 后又调用 `closeSet()`（上次改的 openBox 恢复书签），互相冲突导致收起无效
- **CSS 重构**（`css/style.css`）：
  - `.tool-all`：absolute+translateY(-120%) → `position:relative; margin-bottom:24px`（时间回到文档流，位于搜索框上方）
  - `.mark, .set`：absolute+margin-top:200px → `position:relative; margin-top:24px`（书签/设置面板位于搜索框下方，垂直排列不遮挡）
  - 删除 `.box .all-search{opacity:0}`（书签展开时搜索框不再透明，搜索框与快捷方式共存）
  - 新增 `.onsearch .mark{opacity:0;pointer-events:none}`（搜索聚焦时书签淡出，避免遮挡搜索建议）
  - `mobile.css`：<1200px 时 `.mark,.set` 的 margin-top 由 180px 改为 16px
- **JS 重构**（`js/set.js`）：
  - `openBox()`/`closeBox()` 精简为只切换书签显示（不再操作时间位移/背景模糊，由 CSS 状态接管）
  - `openSet()`/`closeSet()`：closeSet 不再自动 openBox，书签状态由调用方控制
  - 时间点击：改用 `.mark` 的 display 值判断（不用 :visible，避免依赖 offsetWidth），收起时 `closeBox()+blurWd()`，展开时 `closeSet()+openBox()`
  - `.close_sou`（点空白）与 `#menu` 点击：设置原本打开时关闭后恢复书签展示
- **验证**：jsdom 模拟完整交互（加载→收起→展开→设置开/关→点空白），14 项断言全部 PASS

## 新增：Dock 快捷栏（React Bits Dock 原生 JS 复刻）

- 项目为纯静态站（无 React/构建工具），用户选择"原生 JS 复刻"方案
- **新增文件**：
  - `css/dock.css`：面板固定高 68px、深色 #120F17 + 边框 + 毛玻璃、图标项、label tooltip（opacity+translateY 过渡）、小屏适配
  - `js/dock.js`：磁性放大（距离 200px 内线性插值 50→70px）、弹簧动画（半隐式欧拉，参数 mass 0.1/stiffness 150/damping 12 与 React Bits 一致）、面板高度弹簧（68→256）、label 悬停/键盘 focus 显示、Enter/Space 可访问、data-action 点击委托
- **index.html**：head 引入 dock.css；body 末尾加入 5 个 dock-item（搜索/快捷方式/GitHub/知乎/设置，用 iconfont 图标）；引入 dock.js
- **布局**：`.dock-outer` fixed 底部全宽 + pointer-events none，`.dock-panel` absolute 居中 + pointer-events auto；图标放大时向上溢出固定高度面板（浮出效果）
- **验证**：jsdom 确认 5 个图标项、dock-outer 初始高度、原生 click 事件下设置开/关/快捷方式切换/搜索聚焦/键盘 Enter 全部生效；注意 jQuery `trigger('click')` 合成事件不触发原生 addEventListener 委托（测试模拟差异，真实点击无此问题）

## 改造：Dock 改为三页导航（首页/快捷方式页/设置页）

- **需求**：Dock 三页导航；打开页面默认只有搜索栏（不显示快捷方式）；按 Dock 快捷方式才显示；切换时时间/搜索框位置不上移
- **index.html**：Dock 缩为 3 个按钮——首页(icon-home)/快捷方式(icon-wangluo)/设置(icon-shezhi)；`.mark` 恢复 `style="display:none"` 默认隐藏
- **css/style.css**：移除所有位移规则——`.onsearch .all-search` 的 translateY(-140%)、`.onsearch .tool-all` 的 translateY(-140%)、`.onsearch .search-engine` 的 translateY(-38%)；搜索聚焦仅保留背景色变化；时间/搜索框在任何状态下位置固定
- **js/set.js**：① document.ready 移除 `openBox()`（默认首页）；② 删除时间点击的快捷方式切换 handler；③ `#menu` 点击关闭分支去掉 openBox()（不再"恢复快捷方式"）；④ `.close_sou` 点空白只 blurWd+closeSet
- **js/dock.js**：ACTIONS 重写为 home（closeBox+closeSet+blurWd 回首页）/ shortcuts（toggle 快捷方式，展开前先 closeSet）/ settings（复用 menu.click() 含内容加载）
- **验证**：jsdom 12 项断言全 PASS（默认首页、三页切换、互斥、时间点击无副作用）

## 新增：搜索引擎切换改为弧形滚轮（React Bits OptionWheel 原生复刻）

- **新增文件**：
  - `css/option-wheel.css`：`.ow-overlay` 浮层（搜索框上方弹出、深色毛玻璃、480x230px）+ `.option-wheel` 滚轮 + `.option-wheel__item`（color-mix 选中变色）
  - `js/option-wheel.js`：通用组件 `initOptionWheel(container, opts)`——rAF 指数平滑（smoothing 200ms）、弧形布局（R=rowH/tiltRad，y=R·sin，x=-mirror·R·(1-cos)·curve，rot 倾斜）、滚轮（step 钳制 1 档/次 + 140ms 吸附）/拖拽（pointer capture）/键盘上下键/点击选项；返回 open/close/select/getSelected/destroy；open() 同步渲染首帧避免闪烁
- **index.html**：移除 `.search-engine` 下拉面板，加入 `#engine-wheel` 浮层容器；引入 option-wheel.css/js
- **js/set.js**：删除 se-li 下拉逻辑；`.se` 图标点击 → toggle 滚轮（document 委托，点击外部关闭）；滚轮数据来自 `getSeList()`（11 个引擎），默认定位 `getSeDefault()`；onSelect 更新搜索框 action/name/图标（复用原逻辑，不存 cookie）
- **验证**：jsdom 8 项断言全 PASS（打开选中默认、滚轮/键盘/点击选择、action 更新、选完自动关闭、非循环到末尾停住）；注意 jsdom 中键盘/滚轮后选中 class 由 rAF 帧更新，测试需等待动画帧

## 修复：温度显示接口（yiketianqi → wttr.in）

- **根因**：yiketianqi 接口①响应头**无 `Access-Control-Allow-Origin`**（浏览器 fetch 跨域被 CORS 拦截，curl 200 但浏览器用不了）②仅支持 http（https 页面 mixed content 拦截）——双重失效，温度恒为 N/A
- **修复**（`js/main.js`）：改用 **wttr.in**（`https://wttr.in/?format=j1`）——HTTPS + `Access-Control-Allow-Origin: *` + 无需 key + IP 自动定位
  - 数据映射：`weatherDesc[0].value`（英文）→ 中文映射表 WEATHER_ZH（Sunny→晴、Partly cloudy→多云 等 23 项）；`weather[0].maxtempC/mintempC` → tem1/tem2
  - 加 8 秒 AbortController 超时 + catch 错误处理（失败保持 N/A 不报错）
  - 注意：`lang=zh` 参数对 wttr.in 不生效（weatherDesc 仍英文），需本地映射
- **验证**：真实请求端到端（Mist→雾 21°C~22°C）+ jsdom mock 4 项断言全 PASS

## 布局：时间+搜索框固定在页面上部（消除面板切换时的上下跳动）

- **根因**：`#content` 原为 flex 垂直居中（justify-content:center）——快捷方式/设置面板显示时内容总高变大整体居中上移，面板收起时又降回中心，产生"下降动作"
- **修改**（`css/style.css`）：
  - `#content`：`justify-content: flex-start`（顶部排列）
  - `.con`：`justify-content: flex-start` + `padding-top: 15vh`（时间+搜索框固定页面上部，面板在下方展开/收起互不影响）
  - `.sou`：加 `position: relative`（顺带修复引擎滚轮浮层 `.ow-overlay` 的定位——原包含块是 section 导致滚轮定位在视口外）
  - `#keywords` 搜索建议：`top: 46.5%`（视口）→ `top: calc(100% + 6px)`（紧贴搜索框下方）；动画从 `down`（top 44%→46% 下沉）改为 `fade`（纯透明度）
- **验证**：jsdom 回归 10 项 PASS（布局属性 + Dock 三页 + 引擎滚轮；15vh 显示为 115.2px 是 vh 换算，FontFace 报错是 jsdom 无此 API）

## 修复：引擎滚轮滑动无效

- **根因**：① 滚轮事件只监听在浮层 `.option-wheel` 上，鼠标不在浮层上滚动完全无效；② `onSelect` 回调里 `closeEngineWheel()` 导致**每次选中变化浮层立即关闭**——滚动一格就关闭，无法连续滑动（体验上像"没效果"）；③ 平滑滚动 deltaY 小（如 10），step 每次 0.3 几乎看不到移动
- **修复**（`js/option-wheel.js` + `js/set.js`）：
  - **全局滚轮**：浮层 `open()` 时注册 `document` wheel 监听（`onDocWheel`，浮层内事件由自身处理避免双触发），`close()`/`destroy()` 移除——鼠标在页面任意位置滚动都能切换
  - **步进累积**：`wheelAccum += delta/rowH`，整数部分移动、余数保留，单次事件最多 1 格——平滑滚动也能逐级切换
  - **选择语义拆分**：`onSelect`（滚动/拖拽/键盘）只实时应用搜索引擎**不关闭浮层**；新增 `onConfirm` 回调，**点击选项确认**时才关闭——滚动可连续浏览，点击/点外部才收起
- **验证**：jsdom 11 项断言全 PASS（浮层外滚动、连续 3 格切换、浮层保持打开、搜索框实时更新、点击确认关闭、重开定位当前引擎）

## 修复：搜索框位置与各页不一致（flex 方向 bug）

- **根因**：`#content` 是默认 `flex-direction: row`——此前把 `justify-content: flex-start` 只作用于**水平方向**，垂直方向仍由 `align-items: center` 控制（垂直居中）→ 搜索框实际一直在页面下方（居中），面板切换时上下跳动，首页与其他页面位置不一致
- **修复**（`css/style.css`）：`#content` 加 `flex-direction: column`，让 `justify-content: flex-start` 真正控制垂直方向；配合 `.con { padding-top: 15vh }` → 时间+搜索框固定在视口上部，面板开合位置纹丝不动，首页/快捷方式页/设置页完全一致
- **教训**：flex 布局调整垂直位置时必须确认主轴方向（row 时垂直归 align-items 管）；之前 jsdom 只验证了属性值没验证方向，导致 bug 潜伏
- **验证**：jsdom 12 项断言全 PASS（flex-direction/justify-content/align-items、面板开合后 con 属性不变、Dock 三页回归）

## 改造：引擎选择器迷你化（大黑框 → 图标区 hover 极小选择器）

- **需求**：在引擎图标极小区域内动画交互切换；原 480×230px 大浮层太大、交互不灵敏
- **改动**（`css/option-wheel.css` + `js/set.js`）：
  - `.ow-overlay`：480×230 → `width: fit-content; max-width:150px; height:170px`，贴图标左上角（`left:-4px; bottom:calc(100%+8px)`），小圆角
  - 滚轮参数迷你化：fontSize 0.8rem、inset 8、blur 1、fade 0.3、smoothing 150（更灵敏）
  - 交互改为 **hover**：`mouseenter` 显示、`mouseleave` 延迟 150ms 收起（避免图标→浮层移动闪烁）；点击选项确认收起；点击外部收起；全局滚轮/拖拽/键盘保留
  - 图标 hover 放大动画（scale 1.18）+ 切换引擎时图标弹跳动画（scale 1.3 → 复位）
  - **状态一致性**：新增 `engineCurrent` 变量追踪当前实际引擎（onSelect 实时更新）；重开选择器定位到当前引擎而非默认 cookie——修复"选中项与实际搜索框不一致、点击当前项无反应"的 bug
- **验证**：jsdom 11 项断言全 PASS（hover 显隐、滚动切换、离开收起、重开定位当前引擎、点击当前项一致收起、点击切换应用）

## 改造：彻底移除引擎选择器黑框（图标区域直接交互）

- **需求**：不要任何黑框浮层——直接在引擎图标极小区域交互切换
- **改动**：
  - `index.html`：删除 `#engine-wheel` 浮层容器、移除 option-wheel.css/js 引用；`.se` 内新增 `#engine-name`（纯文字引擎名提示，无背景框）
  - `css/style.css`：`.se` 加 `position:relative` + hover 放大（scale 1.18）/focus 轮廓/active 缩放；`#engine-name` 样式（白色文字 + 阴影，opacity/translateY 过渡，`.show` 1.2s 后淡出）
  - `js/set.js`：删除 initOptionWheel 及浮层逻辑，重写为轻量切换——`applyEngine(index)`（循环切换 + 更新 action/name/图标 + 图标弹跳 + 引擎名提示）；hover 图标激活 document wheel（步进累积、单次最多 1 格）、离开停用；图标上 pointer 拖拽切换（>10px 阈值）；键盘上下/左右键
  - `js/option-wheel.js`、`css/option-wheel.css` 文件保留但不再引用
- **验证**：jsdom 11 项断言全 PASS（黑框已移除、滚动/键盘/拖拽切换、循环、离开后滚动不干扰、提示淡出）

## 修复：添加引擎/快捷方式不显示 + 自定义引擎首字显示

- **根因**：① 用户浏览器历史 cookie 损坏（旧数据）→ `getSeList()`/`getQuickList()` 的 `JSON.parse` 无保护直接抛错，添加流程中断；② 引擎切换列表是 ready 时的快照，添加新引擎后不刷新就不生效；③ 自定义引擎 icon 统一为 icon-wangluo 占位，切换按钮显示不出区别
- **修复**（`js/set.js` + `css/style.css`）：
  - `getSeList()`/`getQuickList()` 加 try/catch——cookie 损坏时自动重置为预装列表（与 getBgImg 一致的防护）
  - 引擎切换**动态读取列表**：`applyEngine` 内 `getSeList()` 实时获取、新增 `locateEngineIndex()`（hover/拖拽前定位当前引擎）——添加/删除引擎后无需刷新页面立即生效
  - 新增全局 `renderEngineIcon(icon, title)`：自定义引擎（icon 含 icon-wangluo）在切换按钮显示**名称首字**（class `icon-se-text`，非 iconfont 字体避免乱码），预装引擎正常显示图标；`searchData`/`applyEngine` 统一调用
  - CSS 新增 `.icon-se-text`（font-style normal + 加粗 + flex 居中）
- **验证**：jsdom 12 项断言全 PASS（添加后循环滚动可切到新引擎、首字"测"显示、切回预装图标恢复、快捷方式添加后首页显示、损坏 cookie 自动重置页面不崩）

## 需求：背景保持不动（去掉点击搜索框时的虚化+缩放）

- **根因**：CSS 规则 `.onsearch #bg { transform: scale(1.08); filter: blur(10px) }`——点击搜索框聚焦时背景放大+虚化
- **修复**（`css/style.css`）：删除 `.onsearch #bg` 规则；`#bg` 的 `transition` 简化为 `transition: opacity 1s`（去掉 transform/filter 过渡）——背景任何状态下完全不动（注：JS 中 #bg 仅设置 src 无动效，无需改）
- **验证**：jsdom 5 项断言全 PASS（聚焦前后 transform 保持 scale(1)、filter 保持 blur(0px)）
